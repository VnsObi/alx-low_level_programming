#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 * Return: the absolute value of int
 */
 
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
