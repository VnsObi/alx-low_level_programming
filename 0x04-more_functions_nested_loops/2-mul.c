#include "main.h"

/**
 * mul -  a function that multiplies two integers.
 * Return: the result of the multiplication
 */
 
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
